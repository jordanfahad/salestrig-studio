'use client';

/**
 * Salestrig Studio app mark (replaces the upstream Postiz logo — a third-party
 * trademark that must not be reproduced). A final logo asset can replace this later.
 */
export const Logo = () => {
  return (
    <div
      className="mt-[8px] flex min-h-[52px] min-w-[52px] items-center justify-center rounded-[15px] text-[26px] font-[700] leading-none text-white"
      style={{ background: 'linear-gradient(135deg, #7E4FA8 0%, #4A2C5E 100%)' }}
    >
      S
    </div>
  );
};
